import { CapitalAndSpacePipe } from './capital-and-space.pipe';

describe('CapitalAndSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalAndSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
